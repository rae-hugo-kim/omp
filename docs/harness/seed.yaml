name: cycle-intake-discipline
status: done
completed: 2026-07-30
version: 1
task_id: "20260730-121500-c1in"
goal: >
  1사이클 인테이크 규율을 도입합니다 — 지시를 받는 시점에 1사이클 여부(한 줄 완료 확인
  문장·단수 산출물·1세션 규모)를 판정하고, 미충족이면 구현 전에 분할을 역제안하는 L1
  행동 규칙과 그 기계 백스톱(kickoff-detector 메시지·커밋 경계 넛지)·운영 템플릿(사이클
  큐 scratchboard)을 하네스 배포 세트에 신설합니다.

references:
  - path: rules/cycle_definition.md
    reason: "규칙 본문 — 정의 3항, 구성적 판정, 인테이크 프로토콜, 지시 채널, 예시 뱅크"
  - path: .omp/extensions/harness/index.ts
    reason: "커밋 경계 넛지 배선 (CYCLE_BOUNDARY_NOTE, non-WIP 가드)"
  - path: templates/scratchboard.md
    reason: "사이클 큐 운영 템플릿 — 에이전트 고정 지시 4항 내장"
  - note: >
      설계 근거 — 2026-07-30 하네스 부하 실측: 272k 모델(gpt-5.6-sol)의 228,987 tok
      자동 컴팩션 발동과 마라톤 세션의 재독 복리(단일 파일 166회 재독)를 사이클 단위
      세션 분리로 구조 제거. 사이클 분해의 내부 전례는 sum-vault의 Copybara 보안
      시리즈(권고 1건=PR 1건)와 siksa 정산 단계별 배포.

constraints:
  - 게이트 차단 시맨틱 무변경 — 신설 넛지·리마인더는 전부 advisory
  - "wip: 커밋에는 경계 넛지 미발화 (mid-implementation 보호, context_management.md 정합)"
  - 배포 세트(rules/, templates/, .omp/extensions/harness, AGENTS.md) 안에만 배치

acceptance_criteria:
  - "AC1 rules/cycle_definition.md 신설 — 정의 3항 + 구성적 판정 형식 + 인테이크 프로토콜 + 지시 채널 절 + 예시 뱅크 17건(vault 실사례 출처 병기), rules/INDEX.md 등재"
  - "AC2 AGENTS.md Cycle intake(L1) 조항 + Linked Modules 링크가 실리고 claudedocs/CLAUDEKR.md에 동일 커밋으로 미러된다"
  - "AC3 kickoff-detector 메시지가 인테이크 판정 안내로 교체된다 — 발화/억제(kickoff-done)/비트리거 스모크 3케이스 통과, 트리거 패턴·억제 시맨틱 무변경"
  - "AC4 templates/scratchboard.md 사이클 큐 템플릿 신설(에이전트 고정 지시 4항 내장) + templates/INDEX.md 등재"
  - "AC5 index.ts 커밋 경계 넛지 — 성공한 non-WIP 커밋 tool result에 append, isWipCommit은 게이트 렉서 재사용, 배선 테스트 4건 포함 전체 스위트 그린"

out_of_scope:
  - 게이트 차단 로직 변경 (acceptance/review/backpressure 시맨틱)
  - kickoff-detector 트리거 패턴 변경
  - 컨텍스트 임계 기반 넛지 (Layer 3 — getBranch usage 노출 프로브 후 재검토)

assumptions:
  - harness-sync 배포 화이트리스트가 rules/·templates/·.omp/extensions/harness·AGENTS.md를 포함한다 (scripts/harness-sync.sh:92-122 실측)

risks:
  - 넛지가 사이클 무관 커밋에도 붙음 — 조건부 문구("If this completes...")로 완화 (리뷰 finding low)
  - -F/heredoc 형태 wip 커밋은 렉서 한계로 넛지 발화 — fail-toward-nudge, 리뷰 리포트에 문서화
